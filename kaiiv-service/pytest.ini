# Self-contained, like the AI service: the root pytest.ini drives the
# Playwright suite, and the engine must stay testable on its own for whoever
# ends up maintaining it.
[pytest]
testpaths = tests
